[phases.setup]
nixPkgs = ["php82", "composer", "mysql"]

[phases.build]
cmds = ["composer install --no-interaction --prefer-dist"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
